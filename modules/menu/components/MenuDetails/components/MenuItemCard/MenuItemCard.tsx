import { useLocalSearchParams } from "expo-router";
import React from "react";
import { View } from "react-native";
import { Title } from "react-native-paper";

export const MenuItemCard = () => {
  return (
    <View>
      <Title>fvbdfknv</Title>
    </View>
  );
};
